#include "objectregistry.h"
#include <nap/module.h>


#include <regex>

void registerObjects()
{
//	REG_OPERATOR(spatial::SpatialModulator)
//	REG_OPERATOR(lib::audio::AudioDevice)
//	REG_OPERATOR(AddFloatOperator)
//
//	REG_COMPONENT(nap::PatchComponent)
//	REG_COMPONENT(lib::audio::AudioDevice)
//	REG_COMPONENT(lib::OscComponent)
//	REG_COMPONENT(nap::Transform)
//	REG_COMPONENT(spatial::SpeakerGrid)
//	//    REG_COMPONENT(spatial::SpatialService)
//
//	REG_DATA(std::string)
//
//	REG_DATA(float)
//	REG_CONVERTER<float, std::string>([](auto value, auto& outValue) { outValue = std::to_string(value); });
//	REG_CONVERTER<std::string, float>([](auto value, auto& outValue) { outValue = (float)std::atof(value.c_str()); });
//
////	REG_DATA(bool)
//	REG_CONVERTER<bool, std::string>([](auto value, auto& outValue) { outValue = value ? "1" : "0"; });
//	REG_CONVERTER<std::string, bool>(
//		[](auto value, auto& outValue) { outValue = !(value == "false" || value == "0" || value == "no"); });
//
////	REG_DATA(int)
//	REG_CONVERTER<int, std::string>([](auto value, auto& outValue) { outValue = std::to_string(value); });
//	REG_CONVERTER<std::string, int>([](auto value, auto& outValue) { outValue = std::atoi(value.c_str()); });
}
